<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <import type="com.rayman.v2ex.anotations.PageState" />

        <import type="com.rayman.v2ex.vm.BaseStateVM" />

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="BaseStateVM" />
    </data>

    <LinearLayout android:id="@+id/loading_view"
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/toolbar"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="@{viewModel.state==PageState.LOADING?View.VISIBLE:View.GONE,default=visible}">

        <ProgressBar
            android:id="@+id/progress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
    </LinearLayout>
</layout>